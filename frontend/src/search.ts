export function handleSearch(place: string) {
    console.log(`Search button clicked ${place}`);
    try {
        
    } catch (error) {
        console.error("Error occurred while searching:", error);
        
    }
}
